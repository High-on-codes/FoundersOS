<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>FounderOS — Your AI Chief of Staff</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,500;0,9..144,600;1,9..144,400;1,9..144,500&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
<style>
  :root{
    --ink:#0B0B0A;
    --panel:#141412;
    --line:rgba(242,239,233,0.10);
    --line-strong:rgba(242,239,233,0.18);
    --gold:#C9A961;
    --gold-bright:#E7CD93;
    --paper:#F2EFE9;
    --muted:#8C8778;
    --ease:cubic-bezier(.19,1,.22,1);
  }
  *{box-sizing:border-box;}
  html{scroll-behavior:smooth;}
  body{
    margin:0;
    background:var(--ink);
    color:var(--paper);
    font-family:'Inter',sans-serif;
    line-height:1.6;
    -webkit-font-smoothing:antialiased;
    cursor:default;
  }
  h1,h2,h3{
    font-family:'Fraunces',serif;
    margin:0;
    font-weight:500;
    letter-spacing:-0.01em;
  }
  em{font-style:italic;color:var(--gold);}
  .label{
    font-family:'Inter',sans-serif;font-size:11px;font-weight:600;
    letter-spacing:0.16em;text-transform:uppercase;color:var(--gold);
  }
  a{color:inherit;text-decoration:none;}
  .wrap{max-width:1160px;margin:0 auto;padding:0 40px;}
  ::selection{background:var(--gold);color:var(--ink);}
  :focus-visible{outline:1px solid var(--gold); outline-offset:3px;}

  /* ---------- SCROLL PROGRESS ---------- */
  .progress-bar{
    position:fixed;top:0;left:0;height:2px;
    background:linear-gradient(90deg,var(--gold),var(--gold-bright));
    width:0%;z-index:100;
    transition:width .1s linear;
  }

  /* ---------- REVEAL SYSTEM ---------- */
  .reveal{
    opacity:0;
    transform:translateY(28px);
    transition:opacity 1s var(--ease), transform 1s var(--ease);
  }
  .reveal.in-view{opacity:1;transform:translateY(0);}
  .reveal-line{
    position:relative;overflow:hidden;
  }
  .reveal-line::after{
    content:"";position:absolute;left:0;bottom:0;height:1px;width:0;
    background:var(--line-strong);
    transition:width 1.1s var(--ease);
  }
  .reveal-line.in-view::after{width:100%;}

  /* ---------- NAV ---------- */
  header{
    position:sticky;top:0;z-index:50;
    background:rgba(11,11,10,0.88);
    backdrop-filter:blur(10px);
    border-bottom:1px solid var(--line);
  }
  nav{
    display:flex;align-items:center;justify-content:space-between;
    padding:26px 40px;max-width:1160px;margin:0 auto;
  }
  .logo{
    display:flex;align-items:center;gap:11px;
    font-family:'Fraunces',serif;font-weight:500;font-size:20px;letter-spacing:0.01em;
  }
  .logo-mark{
    width:9px;height:9px;border-radius:50%;
    border:1px solid var(--gold);
    position:relative;
  }
  .logo-mark::after{
    content:"";position:absolute;inset:2.5px;border-radius:50%;background:var(--gold);
  }
  .nav-links{display:flex;gap:44px;align-items:center;}
  .nav-links a:not(.btn){
    font-size:12.5px;color:var(--muted);letter-spacing:0.05em;text-transform:uppercase;
    transition:color .3s var(--ease);
    position:relative;padding-bottom:4px;
  }
  .nav-links a:not(.btn)::after{
    content:"";position:absolute;left:0;bottom:0;width:0;height:1px;background:var(--gold);
    transition:width .35s var(--ease);
  }
  .nav-links a:not(.btn):hover{color:var(--paper);}
  .nav-links a:not(.btn):hover::after{width:100%;}

  .btn{
    display:inline-flex;align-items:center;gap:8px;
    padding:13px 26px;position:relative;overflow:hidden;
    font-size:12.5px;font-weight:600;letter-spacing:0.06em;text-transform:uppercase;
    font-family:'Inter',sans-serif;
    cursor:pointer;border:1px solid transparent;
    transition:transform .4s var(--ease), border-color .4s var(--ease), color .4s var(--ease);
  }
  .btn::before{
    content:"";position:absolute;top:0;left:-120%;width:60%;height:100%;
    background:linear-gradient(115deg, transparent, rgba(255,255,255,0.35), transparent);
    transform:skewX(-20deg);
    transition:left .7s var(--ease);
  }
  .btn:hover::before{left:130%;}
  .btn-primary{
    background:var(--gold);color:#1A1305;
  }
  .btn-primary:hover{transform:translateY(-2px);}
  .btn-ghost{
    background:transparent;color:var(--paper);border:1px solid var(--line-strong);
  }
  .btn-ghost:hover{border-color:var(--gold);color:var(--gold);transform:translateY(-2px);}

  /* ---------- HERO ---------- */
  .hero{padding:110px 0 120px;overflow:hidden;}
  .hero-grid{
    display:grid;grid-template-columns:1.1fr 0.9fr;gap:80px;align-items:center;
  }
  .eyebrow{margin-bottom:28px;display:flex;align-items:center;gap:10px;}
  .eyebrow::before{content:"";width:28px;height:1px;background:var(--gold);}
  .hero h1{
    font-size:56px;line-height:1.12;margin-bottom:28px;font-weight:400;
  }
  .hero p.sub{
    font-size:17px;color:var(--muted);max-width:460px;margin-bottom:40px;font-weight:400;
  }
  .hero-ctas{display:flex;gap:16px;margin-bottom:26px;flex-wrap:wrap;}
  .hero-note{font-size:12.5px;color:var(--muted);letter-spacing:0.03em;}

  .hero-anim{opacity:0;transform:translateY(24px);animation:heroIn 1s var(--ease) forwards;}
  @keyframes heroIn{to{opacity:1;transform:translateY(0);}}

  /* ---- brief card (tilt on mousemove) ---- */
  .brief-panel-outer{
    perspective:1400px;
    opacity:0;transform:translateY(30px);animation:heroIn 1.1s var(--ease) forwards;animation-delay:.5s;
  }
  .brief-panel{
    border:1px solid var(--line-strong);
    transition:transform .5s var(--ease), box-shadow .5s var(--ease);
    transform-style:preserve-3d;
    will-change:transform;
  }
  .brief-panel:hover{
    box-shadow:0 40px 80px -40px rgba(0,0,0,0.7);
  }
  .brief-head{
    padding:22px 30px;border-bottom:1px solid var(--line);
    display:flex;align-items:center;justify-content:space-between;
  }
  .brief-head .label{color:var(--muted);}
  .brief-head .live{color:var(--gold);}
  .brief-head .live .dot{
    display:inline-block;width:5px;height:5px;border-radius:50%;background:var(--gold);
    margin-right:6px;animation:blink 1.8s infinite;
  }
  @keyframes blink{0%,100%{opacity:1;}50%{opacity:.25;}}
  .brief-body{padding:30px;}
  .stream-row{
    display:flex;align-items:baseline;gap:14px;
    font-size:13.5px;color:var(--muted);
    padding:10px 0;border-bottom:1px solid var(--line);
    opacity:0;animation:fadeIn .6s var(--ease) forwards;
  }
  .stream-row:last-of-type{border-bottom:none;}
  .stream-row .src{
    font-size:10px;letter-spacing:0.1em;text-transform:uppercase;color:var(--gold);
    width:74px;flex-shrink:0;font-weight:600;
  }
  .stream-row .val{color:var(--paper);}
  @keyframes fadeIn{from{opacity:0;transform:translateY(3px);}to{opacity:1;transform:translateY(0);}}
  .brief-quote{
    margin-top:26px;padding-top:26px;border-top:1px solid var(--line-strong);
    opacity:0;animation:fadeIn .7s var(--ease) forwards;animation-delay:1.9s;
  }
  .brief-quote .label{display:block;margin-bottom:12px;}
  .brief-quote p{
    font-family:'Fraunces',serif;font-style:italic;font-size:18px;line-height:1.5;
    color:var(--paper);margin:0 0 14px;font-weight:400;
  }
  .brief-quote .time{font-size:11.5px;color:var(--muted);letter-spacing:0.03em;}

  /* ---------- STATS STRIP ---------- */
  .stats-strip{
    border-top:1px solid var(--line);border-bottom:1px solid var(--line);
    display:grid;grid-template-columns:repeat(4,1fr);
  }
  .stat{
    padding:44px 40px;border-right:1px solid var(--line);
    text-align:left;
  }
  .stat:last-child{border-right:none;}
  .stat .num{
    font-family:'Fraunces',serif;font-size:42px;color:var(--gold);font-weight:400;
    display:block;margin-bottom:8px;
  }
  .stat .desc{font-size:12.5px;color:var(--muted);letter-spacing:0.03em;text-transform:uppercase;}

  /* ---------- SECTION SHARED ---------- */
  section{padding:130px 0;}
  .section-head{max-width:620px;margin-bottom:64px;}
  .section-head .eyebrow{margin-bottom:24px;}
  .section-head h2{font-size:38px;line-height:1.2;margin-bottom:18px;font-weight:400;}
  .section-head p{color:var(--muted);font-size:16px;}
  section.bordered{border-top:1px solid var(--line);border-bottom:1px solid var(--line);}

  /* ---------- PROBLEM ---------- */
  .problem-list{
    display:grid;grid-template-columns:1fr 1fr;gap:0;
    border-top:1px solid var(--line);
  }
  .problem-item{
    padding:22px 0;border-bottom:1px solid var(--line);
    display:flex;justify-content:space-between;align-items:baseline;gap:16px;
    font-size:15px;
    transition:padding-left .4s var(--ease), color .4s var(--ease);
  }
  .problem-item:hover{padding-left:10px;color:var(--gold-bright);}
  .problem-item .tool{color:var(--muted);font-size:12px;letter-spacing:0.04em;text-transform:uppercase;}
  .problem-item:nth-child(odd){padding-right:40px;border-right:1px solid var(--line);}
  .problem-item:nth-child(even){padding-left:40px;}
  .problem-item:nth-child(even):hover{padding-left:50px;}

  .questions{margin-top:64px;}
  .q-row{
    display:flex;align-items:baseline;gap:20px;padding:20px 0;border-bottom:1px solid var(--line);
    font-family:'Fraunces',serif;font-size:19px;font-weight:400;
    transition:padding-left .4s var(--ease);
  }
  .q-row:hover{padding-left:12px;}
  .q-row .num{font-family:'Inter',sans-serif;font-size:12px;color:var(--gold);letter-spacing:0.05em;}
  .q-row:first-child{border-top:1px solid var(--line);}

  /* ---------- FEATURES ---------- */
  .feature-grid{
    display:grid;grid-template-columns:repeat(3,1fr);
    border-top:1px solid var(--line);border-left:1px solid var(--line);
  }
  .feature-card{
    border-right:1px solid var(--line);border-bottom:1px solid var(--line);
    padding:44px 36px;min-height:230px;
    display:flex;flex-direction:column;gap:16px;
    transition:background .4s var(--ease), transform .4s var(--ease);
  }
  .feature-card:hover{background:rgba(201,169,97,0.04);transform:translateY(-4px);}
  .feature-card .num{font-family:'Fraunces',serif;font-style:italic;font-size:15px;color:var(--gold);}
  .feature-card h3{font-size:20px;font-weight:400;}
  .feature-card p{font-size:13.5px;color:var(--muted);margin:0;line-height:1.65;}

  /* ---------- INTEGRATIONS ---------- */
  .int-list{columns:3;column-gap:48px;}
  .int-row{
    display:flex;justify-content:space-between;padding:14px 0;border-bottom:1px solid var(--line);
    font-size:14px;break-inside:avoid;
    transition:padding-left .35s var(--ease), color .35s var(--ease);
  }
  .int-row:hover{padding-left:8px;color:var(--gold-bright);}
  .int-row .idx{color:var(--muted);font-size:11px;letter-spacing:0.05em;}

  /* ---------- WHO ---------- */
  .who-row{display:flex;gap:0;flex-wrap:wrap;border-top:1px solid var(--line);}
  .who-chip{
    padding:16px 26px;border-right:1px solid var(--line);border-bottom:1px solid var(--line);
    font-size:13px;letter-spacing:0.04em;text-transform:uppercase;color:var(--muted);
    transition:color .3s var(--ease), background .3s var(--ease);
  }
  .who-chip:hover{color:var(--gold);background:rgba(201,169,97,0.04);}

  /* ---------- PRICING ---------- */
  .pricing-grid{
    display:grid;grid-template-columns:repeat(3,1fr);
    border-top:1px solid var(--line);border-left:1px solid var(--line);
  }
  .price-card{
    border-right:1px solid var(--line);border-bottom:1px solid var(--line);
    padding:44px 34px;display:flex;flex-direction:column;
    transition:transform .4s var(--ease);
  }
  .price-card:hover{transform:translateY(-4px);}
  .price-card.popular{
    background:rgba(201,169,97,0.045);
    box-shadow:inset 0 0 0 1px rgba(201,169,97,0.35);
  }
  .price-card .label{margin-bottom:20px;}
  .price-card h3{font-size:22px;margin-bottom:10px;font-weight:400;}
  .price-card .price{font-family:'Fraunces',serif;font-size:44px;font-weight:400;margin:10px 0 6px;}
  .price-card .price span{font-size:14px;color:var(--muted);font-family:'Inter',sans-serif;}
  .price-card .price-desc{font-size:13.5px;color:var(--muted);margin-bottom:28px;line-height:1.6;}
  .price-card ul{list-style:none;margin:0 0 34px;padding:0;display:flex;flex-direction:column;gap:13px;}
  .price-card li{font-size:13.5px;color:var(--paper);display:flex;gap:12px;align-items:flex-start;}
  .price-card li::before{content:"—";color:var(--gold);flex-shrink:0;}
  .price-card .btn{width:100%;justify-content:center;margin-top:auto;}

  /* ---------- VISION ---------- */
  .vision{text-align:center;padding:150px 0;}
  .vision .label{justify-content:center;margin-bottom:32px;}
  .vision .label::before{display:none;}
  .vision blockquote{
    font-family:'Fraunces',serif;font-size:42px;line-height:1.3;font-weight:400;
    max-width:760px;margin:0 auto 26px;
  }
  .vision p{color:var(--muted);max-width:520px;margin:0 auto;font-size:15px;}

  /* ---------- CTA BAND ---------- */
  .cta-band{text-align:center;padding:130px 0;}
  .cta-band h2{font-size:40px;margin-bottom:20px;font-weight:400;}
  .cta-band p{color:var(--muted);margin-bottom:40px;font-size:16px;}
  .cta-row{display:flex;gap:16px;justify-content:center;flex-wrap:wrap;}

  footer{
    padding:40px 0;border-top:1px solid var(--line);
    display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:12px;
  }
  footer .fine{color:var(--muted);font-size:12px;letter-spacing:0.02em;}

  @media (max-width:920px){
    .hero-grid{grid-template-columns:1fr;}
    .hero h1{font-size:38px;}
    .feature-grid{grid-template-columns:1fr;}
    .problem-list{grid-template-columns:1fr;}
    .problem-item:nth-child(odd){border-right:none;padding-right:0;}
    .problem-item:nth-child(even){padding-left:0;}
    .pricing-grid{grid-template-columns:1fr;}
    .int-list{columns:1;}
    .stats-strip{grid-template-columns:repeat(2,1fr);}
    .stat:nth-child(2){border-right:none;}
    .vision blockquote{font-size:28px;}
    .wrap{padding:0 22px;}
  }
  @media (prefers-reduced-motion: reduce){
    *{animation:none !important;transition:none !important;}
  }
</style>
</head>
<body>

<div class="progress-bar" id="progressBar"></div>

<header>
  <nav>
    <div class="logo"><span class="logo-mark"></span>FounderOS</div>
    <div class="nav-links">
      <a href="#product">Product</a>
      <a href="#integrations">Integrations</a>
      <a href="#pricing">Pricing</a>
      <a href="#" class="btn btn-ghost">Book a Demo</a>
    </div>
  </nav>
</header>

<section class="hero">
  <div class="wrap hero-grid">
    <div>
      <div class="eyebrow hero-anim" style="animation-delay:.05s"><span class="label">Decision Intelligence for Startups</span></div>
      <h1 class="hero-anim" style="animation-delay:.15s">Run your startup with the clarity of <em>someone who already knows everything.</em></h1>
      <p class="sub hero-anim" style="animation-delay:.3s">Connect GitHub, Slack, Stripe, Notion, and more. Receive one daily briefing, instant answers to business questions, and a complete view of your company — from a single, quiet dashboard.</p>
      <div class="hero-ctas hero-anim" style="animation-delay:.42s">
        <button class="btn btn-primary">Join the Waitlist</button>
        <button class="btn btn-ghost">Book a Demo</button>
      </div>
      <div class="hero-note hero-anim" style="animation-delay:.52s">From $79/month. Invitation-based onboarding.</div>
    </div>

    <div class="brief-panel-outer">
      <div class="brief-panel" id="briefPanel">
        <div class="brief-head">
          <span class="label">Morning Brief · 07:12</span>
          <span class="label live"><span class="dot"></span>Live</span>
        </div>
        <div class="brief-body">
          <div class="stream-row" style="animation-delay:.7s"><span class="src">GitHub</span><span class="val">14 pull requests merged, 2 deployments to production</span></div>
          <div class="stream-row" style="animation-delay:1.0s"><span class="src">Stripe</span><span class="val">Monthly revenue up 4.2%, 3 accounts upgraded</span></div>
          <div class="stream-row" style="animation-delay:1.3s"><span class="src">Support</span><span class="val">Two accounts flagged at risk of churn</span></div>
          <div class="stream-row" style="animation-delay:1.6s"><span class="src">Slack</span><span class="val">Engineering blocked on the billing API</span></div>
          <div class="stream-row" style="animation-delay:1.9s"><span class="src">Calendar</span><span class="val">Investor call moved to 3:00 PM</span></div>
          <div class="brief-quote">
            <span class="label">Suggested Priority</span>
            <p>"Engineering is blocked on the billing API ahead of this afternoon's investor call — worth a ten-minute unblock beforehand."</p>
            <div class="time">Compiled from 5 sources this morning</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="stats-strip">
  <div class="stat reveal"><span class="num" data-count="11">0</span><span class="desc">Integrations Supported</span></div>
  <div class="stat reveal"><span class="num" data-count="1">0</span><span class="desc">Briefing, Each Morning</span></div>
  <div class="stat reveal"><span class="num" data-count="47" data-suffix="min">0</span><span class="desc">Saved Per Founder, Daily</span></div>
  <div class="stat reveal"><span class="num" data-count="24" data-suffix="/7">0</span><span class="desc">Continuous Monitoring</span></div>
</div>

<section class="bordered">
  <div class="wrap">
    <div class="section-head reveal">
      <div class="eyebrow"><span class="label">The Problem</span></div>
      <h2>The information already exists. It is simply scattered.</h2>
      <p>Every morning, founders lose the first hour of their day reconstructing what happened while they were away.</p>
    </div>

    <div class="problem-list reveal-line">
      <div class="problem-item reveal"><span>Open pull requests awaiting review</span><span class="tool">GitHub</span></div>
      <div class="problem-item reveal"><span>Unread messages across every channel</span><span class="tool">Slack</span></div>
      <div class="problem-item reveal"><span>Blocked tickets nobody has flagged</span><span class="tool">Linear</span></div>
      <div class="problem-item reveal"><span>This week's revenue movement</span><span class="tool">Stripe</span></div>
      <div class="problem-item reveal"><span>Open tickets from unhappy customers</span><span class="tool">Support</span></div>
      <div class="problem-item reveal"><span>Sprint notes no one has read</span><span class="tool">Notion</span></div>
    </div>

    <div class="questions">
      <div class="q-row reveal"><span class="num">01</span>What happened yesterday?</div>
      <div class="q-row reveal"><span class="num">02</span>What needs my attention today?</div>
      <div class="q-row reveal"><span class="num">03</span>Which customers are unhappy?</div>
      <div class="q-row reveal"><span class="num">04</span>Has revenue changed?</div>
    </div>
  </div>
</section>

<section id="product">
  <div class="wrap">
    <div class="section-head reveal">
      <div class="eyebrow"><span class="label">The Solution</span></div>
      <h2>One dashboard that knows everything happening in your company.</h2>
      <p>FounderOS connects to the tools you already use and turns thousands of scattered events into a single, considered briefing.</p>
    </div>

    <div class="feature-grid">
      <div class="feature-card reveal"><span class="num">I.</span><h3>Daily Executive Brief</h3><p>Revenue, engineering progress, open blockers, customer sentiment, and the day's priorities — before you open a single tool.</p></div>
      <div class="feature-card reveal"><span class="num">II.</span><h3>Ask Anything</h3><p>"Why did churn increase this month?" Ask in plain language and receive one considered answer, drawn from every source, with context.</p></div>
      <div class="feature-card reveal"><span class="num">III.</span><h3>Product Intelligence</h3><p>Feedback from every channel is grouped automatically. Recurring problems and requests surface on their own.</p></div>
      <div class="feature-card reveal"><span class="num">IV.</span><h3>Engineering Insights</h3><p>Merged pull requests, deployments, failed builds, and release summaries — without opening GitHub.</p></div>
      <div class="feature-card reveal"><span class="num">V.</span><h3>Business Overview</h3><p>Revenue, growth, customer activity, and product health, held together in one considered view.</p></div>
      <div class="feature-card reveal"><span class="num">VI.</span><h3>AI-Powered Reports</h3><p>Investor updates, weekly summaries, and sprint reports, drawn from the same data behind your morning brief.</p></div>
    </div>
  </div>
</section>

<section class="bordered" id="integrations">
  <div class="wrap">
    <div class="section-head reveal">
      <div class="eyebrow"><span class="label">Integrations</span></div>
      <h2>Works with what you already use.</h2>
      <p>No workflow to relearn. FounderOS sits quietly above your existing tools as the intelligence layer that connects them.</p>
    </div>
    <div class="int-list">
      <div class="int-row reveal"><span>GitHub</span><span class="idx">01</span></div>
      <div class="int-row reveal"><span>Slack</span><span class="idx">02</span></div>
      <div class="int-row reveal"><span>Linear</span><span class="idx">03</span></div>
      <div class="int-row reveal"><span>Jira</span><span class="idx">04</span></div>
      <div class="int-row reveal"><span>Notion</span><span class="idx">05</span></div>
      <div class="int-row reveal"><span>Stripe</span><span class="idx">06</span></div>
      <div class="int-row reveal"><span>Google Workspace</span><span class="idx">07</span></div>
      <div class="int-row reveal"><span>Gmail</span><span class="idx">08</span></div>
      <div class="int-row reveal"><span>Google Calendar</span><span class="idx">09</span></div>
      <div class="int-row reveal"><span>Support Platforms</span><span class="idx">10</span></div>
      <div class="int-row reveal"><span>Analytics Tools</span><span class="idx">11</span></div>
    </div>
  </div>
</section>

<section>
  <div class="wrap">
    <div class="section-head reveal">
      <div class="eyebrow"><span class="label">Who It's For</span></div>
      <h2>Built for teams too small for a Chief of Staff.</h2>
      <p>Especially startups between 5 and 100 people, where the founder still holds the entire picture in their head.</p>
    </div>
    <div class="who-row reveal-line">
      <div class="who-chip reveal">Startup Founders</div>
      <div class="who-chip reveal">CEOs</div>
      <div class="who-chip reveal">CTOs</div>
      <div class="who-chip reveal">Product Managers</div>
      <div class="who-chip reveal">Small Founding Teams</div>
    </div>
  </div>
</section>

<section class="bordered" id="pricing">
  <div class="wrap">
    <div class="section-head reveal">
      <div class="eyebrow"><span class="label">Pricing</span></div>
      <h2>Considered plans for early-stage teams.</h2>
      <p>Every plan includes unlimited integrations and the daily brief. Upgrade for deeper history, more seats, and fuller reporting.</p>
    </div>

    <div class="pricing-grid">
      <div class="price-card reveal">
        <span class="label">Starter</span>
        <h3>Solo Founder</h3>
        <div class="price">$79<span> / month</span></div>
        <div class="price-desc">For a single founder holding every function.</div>
        <ul>
          <li>Daily executive brief</li>
          <li>Ask Anything — 500 queries monthly</li>
          <li>Up to 5 integrations</li>
          <li>7-day data history</li>
        </ul>
        <button class="btn btn-ghost">Start Trial</button>
      </div>

      <div class="price-card popular reveal">
        <span class="label">Most Chosen</span>
        <h3>Growth</h3>
        <div class="price">$249<span> / month</span></div>
        <div class="price-desc">For founding teams making decisions together.</div>
        <ul>
          <li>Everything in Starter</li>
          <li>Unlimited Ask Anything</li>
          <li>Unlimited integrations</li>
          <li>Product Intelligence & trend detection</li>
          <li>Investor and sprint reports</li>
          <li>Up to 10 seats</li>
        </ul>
        <button class="btn btn-primary">Start Trial</button>
      </div>

      <div class="price-card reveal">
        <span class="label">Scale</span>
        <h3>Enterprise</h3>
        <div class="price">Bespoke</div>
        <div class="price-desc">For teams beyond 50 people, with more to hold together.</div>
        <ul>
          <li>Everything in Growth</li>
          <li>Unlimited seats</li>
          <li>Custom data retention</li>
          <li>SSO and audit logs</li>
          <li>Dedicated onboarding</li>
        </ul>
        <button class="btn btn-ghost">Talk to Us</button>
      </div>
    </div>
  </div>
</section>

<section class="vision">
  <div class="wrap reveal">
    <div class="label">The Vision</div>
    <blockquote>Every startup deserves a <em>Chief of Staff.</em> Most cannot yet hire one.</blockquote>
    <p>FounderOS fills that role — watching over your business, surfacing what matters, and returning the hour you once spent gathering information, so you may spend it deciding instead.</p>
  </div>
</section>

<section class="cta-band bordered">
  <div class="wrap reveal">
    <h2>Spend less time gathering. More time deciding.</h2>
    <p>Join the founders receiving one briefing, in place of ten dashboards.</p>
    <div class="cta-row">
      <button class="btn btn-primary">Join the Waitlist</button>
      <button class="btn btn-ghost">Book a Demo</button>
    </div>
  </div>
</section>

<footer>
  <div class="wrap" style="display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:12px;">
    <div class="logo" style="font-size:15px;"><span class="logo-mark"></span>FounderOS</div>
    <div class="fine">© 2026 FounderOS. The intelligence layer above your tools.</div>
  </div>
</footer>

<script>
  // Scroll progress bar
  const progressBar = document.getElementById('progressBar');
  function updateProgress(){
    const h = document.documentElement;
    const scrolled = (h.scrollTop) / (h.scrollHeight - h.clientHeight) * 100;
    progressBar.style.width = scrolled + '%';
  }
  document.addEventListener('scroll', updateProgress, {passive:true});

  // Reveal-on-scroll with stagger per container
  const revealEls = document.querySelectorAll('.reveal, .reveal-line');
  const io = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if(entry.isIntersecting){
        const el = entry.target;
        const parent = el.parentElement;
        let idx = 0;
        if(parent){
          idx = Array.prototype.indexOf.call(parent.children, el);
        }
        el.style.transitionDelay = Math.min(idx * 70, 500) + 'ms';
        el.classList.add('in-view');
        io.unobserve(el);
      }
    });
  }, {threshold:0.15, rootMargin:'0px 0px -60px 0px'});
  revealEls.forEach(el => io.observe(el));

  // Number count-up for stats
  const statEls = document.querySelectorAll('.stat .num');
  const statIo = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if(entry.isIntersecting){
        const el = entry.target;
        const target = parseInt(el.getAttribute('data-count'), 10);
        const suffix = el.getAttribute('data-suffix') || '';
        const duration = 1400;
        const start = performance.now();
        function tick(now){
          const p = Math.min((now - start) / duration, 1);
          const eased = 1 - Math.pow(1 - p, 3);
          el.textContent = Math.round(eased * target) + suffix;
          if(p < 1) requestAnimationFrame(tick);
        }
        requestAnimationFrame(tick);
        statIo.unobserve(el);
      }
    });
  }, {threshold:0.5});
  statEls.forEach(el => statIo.observe(el));

  // Subtle 3D tilt on the brief panel, like a product-configurator card
  const panel = document.getElementById('briefPanel');
  const panelOuter = panel.closest('.brief-panel-outer');
  panelOuter.addEventListener('mousemove', (e) => {
    const rect = panel.getBoundingClientRect();
    const x = (e.clientX - rect.left) / rect.width - 0.5;
    const y = (e.clientY - rect.top) / rect.height - 0.5;
    panel.style.transform = `rotateY(${x * 5}deg) rotateX(${-y * 5}deg)`;
  });
  panelOuter.addEventListener('mouseleave', () => {
    panel.style.transform = 'rotateY(0deg) rotateX(0deg)';
  });
</script>

</body>
</html>
